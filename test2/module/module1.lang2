import module::module2::{pow_inner, Dir};

type File struct {
    name: *string,
    created_at: int,
    parent: *Dir,
};

fn pow(n: int, a: int): int = {
    if a <= 1 {
        n
    } else {
        pow_inner n a
    }
}
